# --- Server-side secrets ($env/static/private) ---
# Google Calendar service account (read-only) — powers Shows
MISSY_CALENDAR_CLIENT_EMAIL=service-account@your-project.iam.gserviceaccount.com
MISSY_CALENDAR_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Cloudflare Turnstile secret (server-side validation of the contact form)
# Local dev test key that always passes: 1x0000000000000000000000000000000AA
MISSY_TURNSTILE_SECRET_KEY=1x0000000000000000000000000000000AA

# Behold.so Instagram feed ID. Create a feed at behold.so connected to @missy.midwest,
# then copy its ID (the feed is served at https://feeds.behold.so/<id>). Fetched
# server-side and validated; when unset, the Home page shows the placeholder grid.
BEHOLD_FEED_ID=

# --- Sentry error monitoring (errors only) ---
# Server-side DSN ($env/dynamic/private). When set, server errors + feed failures report.
SENTRY_DSN=
# Client-side DSN (public). Usually the same DSN value as above.
PUBLIC_SENTRY_DSN=
# Optional environment label shown in Sentry. Left unset the SDK reports
# "development", so only deploys that set this (per Netlify context) count as
# production traffic.
SENTRY_ENVIRONMENT=
PUBLIC_SENTRY_ENVIRONMENT=
# Build-time source map upload (set these in Netlify, not locally). Without the
# auth token the Vite plugin is a no-op, so builds never fail for missing creds.
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# --- Client-side (import.meta.env, VITE_ prefix is public) ---
# Cloudflare Turnstile site key. Local always-pass test key: 1x00000000000000000000AA
VITE_TURNSTILE_SITE_KEY=1x00000000000000000000AA

# --- Stripe shop + transactional email ---
# Stripe restricted key (rk_...): Products read+write, Prices read, Checkout write, Payment Intents read.
# Test-mode key for local/dev; swap in the live key at go-live.
STRIPE_SECRET_KEY=rk_test_...
# Webhook signature secret (Stripe CLI `stripe listen` prints one for local dev).
STRIPE_WEBHOOK_SECRET=whsec_...
# Resend transactional email.
RESEND_API_KEY=re_...
# From address — must be on a Resend-verified domain.
RESEND_FROM_EMAIL=noreply@yourdomain.com
# Where order notifications go (Missy).
ORDER_NOTIFY_EMAIL=missy@example.com
# Where contact-form messages go (may be the same inbox).
CONTACT_TO_EMAIL=missy@example.com
