.DS_Store
node_modules
.pnpm-store
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
.vercel
.output

.netlify

credentials.json

.superpowers/
# docs/ is a TRACKED knowledge base (runbooks, ops, product notes) — see docs/README.md.
# Only the transient superpowers planning docs/specs are ignored:
docs/superpowers/
.claude/worktrees/

/test-results
/playwright-report
/.playwright
/coverage

# Playwright MCP scratch output
/.playwright-mcp/
# Local-only photo candidates / shareable originals (not for the build)
/bio-candidates
# Raw photo/video sources under shop/ — keep local as backups; ship the optimized .webp
static/shop/**/*.JPG
static/shop/**/*.jpg
static/shop/**/*.jpeg
static/shop/**/*.JPEG
static/shop/**/*.MP4
static/shop/**/*.mp4
static/shop/**/*.MOV
static/shop/**/*.mov
