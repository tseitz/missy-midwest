engine-strict=true
confirm-modules-purge=false
# Don't let `pnpm <script>` auto-reinstall/recreate node_modules before running
# (that recreate, interrupted, can wipe node_modules). Warn on stale deps instead.
verify-deps-before-run=warn
