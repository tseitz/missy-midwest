engine-strict=true
confirm-modules-purge=false
# NOTE: the stale-deps guard (verifyDepsBeforeRun) lives in pnpm-workspace.yaml,
# not here — pnpm 10/11 only reads auth/registry settings from .npmrc, so a
# `verify-deps-before-run=` line here is silently ignored.
