export const SITE_NAME = 'Missy Midwest';
export const DEFAULT_DESCRIPTION =
	'Missy Midwest — open-format DJ, producer & vocalist. Music, shows, merch & booking.';
export const DEFAULT_OG_IMAGE = '/landing/missy-fan-crop.webp';

/** Public social profile URLs (JSON-LD `sameAs`) — single source in `$lib/social`. */
export { SOCIAL_URLS } from '$lib/social';
