const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			colors: {
				missy: {
					400: '#88428c',
					500: '#4f2449'
				}
			}
		}
	},

	plugins: []
};

module.exports = config;
